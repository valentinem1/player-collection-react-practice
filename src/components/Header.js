import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <div className="app-header">
                <header className="header-logo">NBA COLLECTION</header>
            </div>
        );
    }
}

export default Header;